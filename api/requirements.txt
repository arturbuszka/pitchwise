# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12          # uploady plików
pydantic>=2.9
pydantic-settings>=2.6

# --- DB ---
sqlmodel>=0.0.22
aiosqlite>=0.20                   # async SQLite (MVP)

# --- Kolejka zadań ---
arq>=0.26
redis>=5.2

# --- LLM (provider-agnostic: OpenAI-compatible chat API przez HTTP) ---
httpx>=0.27

# --- Vision (cięższe; instalacja może chwilę zająć) ---
ultralytics>=8.3                  # YOLO11
supervision>=0.25                 # ByteTrack, annotators
opencv-python-headless>=4.10
numpy>=1.26
ffmpeg-python>=0.2                # wrapper na ffmpeg CLI (ffmpeg musi być w PATH)
